/*
pointers are variable which are used to store adress of data
two types
1 Data variable   for eg int x=10;
2 adress variable int *p; // declartion 
                  p=&x;  // initialisation
                 cout<<*p; //dereferencing
                


                let int x=0;
                    int *p;
                    p=&x;
                    cout<<x;   =10
                    cout<<&x;  =200 //assuming 2bytes
                    cout<<p;   =200 
                    cout<<&p;  =300 //assuming 3 bytes
                    cout<<*p;  =10  
*/