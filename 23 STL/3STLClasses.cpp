/*
Algorithms 
Containers 
Iterators

alogrithms are meant for managing containers
arrays,linkedlists,stacks,queues all are containers.
For accessing containers itertors are available

algorithms are for
search
sort 
performing binary search 
reverse
concat
copy
union 
intersection
merge 
Heap  etc


Containers 
all these are Template classes means they are generic(can work for any type of data)
vector(it is self managed array which can manage size of array)  
functions of vectors----push_back()-to insert element at last
                        pop_back()- to remove the last element
                        insert()- to insert an element
                        remove()
                        size()
                        emmpty()


                        ##linked list##
list (doubly linked list)
functions of list   ----push_front()-to insert element at front
                        pop_front()-to remove element at front
                        push_back()-to insert element at last
                        pop_back()- to remove the last element
                        insert()- to insert an element
                        remove()
                        size()
                        emmpty()
                        front()
                        back()

forward_list(singly linked list)
functions of forwarad list      ----push_front()-to insert element at front
                                    pop_front()-to remove element at front
                                    push_back()-to insert element at last
                                    pop_back()- to remove the last element
                                    insert()- to insert an element
                                    remove()
                                    size()
                                    emmpty()
                                    front()
                                    back()

                        ##linked list##


deque(same as vector, double ended queue)
functions of deque   ----push_front()-to insert element at front
                        pop_front()-to remove element at front
                        push_back()-to insert element at last
                        pop_back()- to remove the last element
                        insert()- to insert an element
                        remove()
                        size()
                        emmpty()
                        front()
                        back()


priority_queue(for heap data structure)(always the largest element from queue will be deleted,for insertion we do not bother)
functions of priority queue----push()
                               pop()
                               empty()
                               size()


stack(LIFO)
functions of stack         ----push()
                               pop()
                               empty()
                               size()


set(contains only unique element)

multiset(allows duplicate element)

Map(useful for storing key value pair)(uses Hash Table)
<key;value> pair
example
key   value
1    "John"
2    "Harsh"
3    "Random"

MultiMap(keys can be dupicated but values should be different)
<key value> pair 

*/