/*
#include<fstream>
int main()
{
    ofstream outfile("My.txt"); //whatever we give to this file will be stored in that file
    //if file does not exist, it will create new file and if the file already exist and some content is there and if we need that content then we will append the content
    //Two modes exist - ios::app
                        // ios::trunc //default we sholud take this
    outfile<<"Hello"<<endl;
    outfile<<25<<endl;
    outfile.close();
}
*/