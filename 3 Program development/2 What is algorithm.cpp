/*Algorithm it is a step by step procedure to solve an problem
first we have algo then we create problem
pseudo code is code to define  steps of program but it doesnt require to be perfect in syntax
Algorithm example
{
    sum<- 0;
    for each element x iin list
    begin 
    sum <- sum +x ;
    end;
     avg = sum/n;
      return avg;
} 
*/